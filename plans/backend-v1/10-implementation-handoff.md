# Handoff to the implementation task

The repository currently contains plans and source material, not an implemented backend. This handoff starts P00/P01 only. Complete Phase 1 eventually includes P11's management checklist; completing the first packet must not be reported as completing the bot. This planning task remains the place for architecture revisions and subsequent mini-plans.

## Paste this into a new Codex task attached to this repository

```text
Implement the first foundation increment of the planned memecoin due diligence backend.

Workspace:
C:\Users\Baham\Documents\Nexuz\dove_red\duediligencecasino

Follow the executing host's applicable agent instructions, then read agentic/architecture.md and any active agentic/analysis.md. The author's workspace-specific AGENTS.md and Bridgecode specialists are intentionally omitted from this public repository. Read plans/backend-v1/README.md and every canonical document it indexes. The most recent user-confirmed lifecycle is in 09-two-checklist-lifecycle.md; detailed implementation packets are in 05-implementation-and-validation.md. The original pre-plan and transcript bundle were source material, not competing implementation specifications; they are intentionally omitted from this public repository and are not required to follow the published plan.

First inspect the actual repository. At planning handoff no backend, package manifest or runtime tests existed; do not overwrite work that may have been added since. This task implements code; the architecture/planning task remains separate.

Product scope:
Phase 1 is manual contract-driven due diligence with TWO linked checklists:
1. Entry/thesis assessment across on-chain/market, attention/narrative and social/actor evidence. Save PASS/FAIL with reason class, immutable evidence and the complete inspectable checklist.
2. Reassessment of a saved thesis: validation/invalidation, loss-prevention/exit review and staged DCA-out proposals. Saving a passing entry thesis activates management immediately; age and market-cap bands select rule parameters and cannot delay an early invalidation. Phase 1 optionally records actual manual entries/sales and a journal. Without position data, proposals are explicitly hypothetical. No order is submitted.
Phase 2 is automatic new-pair discovery/analysis. Phase 3 is automatic trading. Neither belongs in this task's implementation scope.

Initial authorized deliverable: complete P00 and P01, then stop and report evidence. Do not attempt all packets at once. Build the minimal TypeScript CLI foundation, runtime schemas, SQLite plus content-addressed artifact storage, explicit policy/config, fixture-backed entry-checklist-to-verdict flow, saved explanation and offline replay groundwork. Preserve a versioned ENTRY/MANAGEMENT snapshot boundary for P11; do not scaffold a whole unused management subsystem now. Missing/unimplemented dependencies yield UNKNOWN, never fixture-filled live results.

Nonnegotiable constraints:
- First chains: Solana, BSC, Base and Robinhood; others explicitly partial. Chain recognition and venue certification are separate. Keep the four chains visible even where access is unavailable.
- Initial monetary data/API budget is zero. Verify free operation/account entitlement before calls, reserve request/credit budget, and prevent paid overage. No subscription, recharge, vendor demo key as production access or automatic paid fallback.
- SQLite is the proposed local store. Do not introduce Convex, multiple services, queues or a graph database without concrete evidence and a scoped plan revision.
- TinyFish Search/Fetch is the first free public-web candidate; Parallel Search/Extract is a benchmark alternative, not a second mandatory dependency. Direct APIs/imports remain independent.
- Gemini 3.5 Flash-Lite at high thinking is the preferred primary semantic extractor. P01 uses fixtures/import. P06 must read the user's forthcoming model document, verify current API/SDK syntax, free access and labeled extraction quality before hosted activation. Do not silently choose another model or thinking level.
- Code owns quantities, feature arithmetic, checklist outcomes and final results. LLMs supply bounded cited semantic observations. Replay freezes those outputs.
- Preserve source/availability timestamps, cap versus FDV, token creation versus pool age, quote versus stateful simulation, and missing versus zero. Do not invent age/risk/exit thresholds from examples. Null settings remain explicit.
- No UI, posting/shilling, signing keys, transaction broadcasting, automatic scanner, portfolio construction, background stop-loss or paper execution. Manual position support is later P11, not custody.

Validation:
Use P00/P01 acceptance in 05. Pin a compatible runtime/dependency set based on actual Windows evidence. Prove clean install/typecheck/build, real SQLite/artifact round trip, schema validation, all entry verdict branches/precedence, saved hidden checklist retrieval, JSON stdout purity, no network in offline mode and deterministic replay groundwork. Live feasibility probes are bounded, read-only and free; unsupported access is a recorded limitation, not a reason to fabricate capability. Follow the executing host's applicable review process and keep corrections bounded by concrete acceptance findings. Document exact commands and failures honestly.

Finish with: changed files and responsibilities; commands/results; foundation demo instructions; per-chain/provider capability findings; remaining setup/user-owned choices; current checklist/review status; recommended next packet. Update agentic/architecture.md with implemented facts, keep unimplemented proposals labeled, and preserve any unfinished recovery state. Do not claim the whole Phase 1 bot is complete after P01.

If provider/runtime evidence contradicts the plan, make the smallest justified implementation adjustment within scope, record its evidence, finish unaffected work, and return consequential architecture changes to the planning task. Do not replan the entire product or silently broaden this packet.
```

## Continuation order after the foundation passes

| Next increment | Deliverable | Completion boundary |
|---|---|---|
| P02/P03 | Chain identity, token controls, markets and holder coverage | Supported reads with explicit uncertainty |
| P03a/P03b | Chart, macro, chain-activity/bridge/launch context | Causal/time-correct descriptive features |
| P04a–d | Certified venue entry/acquired-quantity exit paths | Per-venue proof and simulation limitations |
| P05/P06 | Evidence corpus, selected retrieval, cited semantics/Gemini | Public/free/permission-safe evidence and evaluated extraction |
| P07 | Integrated entry engine | Four-chain attempt, saved entry checklist and replay |
| P11a–d | Case, thesis, management checklist, optional positions/journal | Complete manual two-checklist Phase 1 |
| P08–P10 | History, actors, evaluation/calibration | Add as data supports; not prerequisites for P11 |

Ask this planning task for a packet-specific mini-plan when a substantive contract needs refinement. Return actual provider failures and validation evidence, not merely “implemented.” The exact age bands, risk tolerances and realization thresholds are explicit user profile settings still to be selected; no architecture change is needed to represent them as unset. The detailed Gemini document is an outstanding input for P06, not a blocker to P00/P01.

## Handoff evidence limits

Primary provider documentation was researched, but no authenticated API, live chain probe, runtime build or strategy evaluation was performed here. Document validation and independent review concern the plan's consistency and suitability for implementation. They do not certify access, predictive performance, sellability or profitable trading.
